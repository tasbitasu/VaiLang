{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run VaiLang Compiler (vai test.vai)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/vai",
            "args": ["test.vai"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "preLaunchTask": "Build VaiLang Compiler (vai)"
        }
    ]
}
